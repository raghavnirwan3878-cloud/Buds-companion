<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- ===================== BASE ===================== -->
    <style name="Theme.BudsCompanion.Base" parent="Theme.Material3.Dark.NoActionBar">
        <item name="colorPrimary">@color/accent</item>
        <item name="colorOnPrimary">@android:color/white</item>
        <item name="colorSurface">@color/surface</item>
        <item name="colorOnSurface">@color/text_primary</item>
        <item name="android:windowBackground">@color/bg_primary</item>
        <item name="android:statusBarColor">@color/bg_primary</item>
        <item name="android:navigationBarColor">@color/bg_primary</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <!-- ===================== CLASSIC ===================== -->
    <style name="Theme.BudsCompanion" parent="Theme.BudsCompanion.Base"/>

    <!-- ===================== LIQUID GLASS ===================== -->
    <style name="Theme.BudsCompanion.LiquidGlass" parent="Theme.BudsCompanion.Base">
        <item name="colorSurface">@color/glass_surface</item>
        <item name="colorOnSurface">@color/glass_text_primary</item>
        <item name="android:windowBackground">@color/glass_bg_primary</item>
        <item name="android:statusBarColor">@color/glass_bg_primary</item>
        <item name="android:navigationBarColor">@color/glass_bg_primary</item>
    </style>

    <!-- Text appearance overrides (no custom font — uses system default) -->
    <style name="TextAppearance.Buds.Headline" parent="TextAppearance.Material3.HeadlineMedium">
        <item name="android:textColor">@color/text_primary</item>
    </style>

    <style name="TextAppearance.Buds.Body" parent="TextAppearance.Material3.BodyMedium">
        <item name="android:textColor">@color/text_primary</item>
    </style>

    <style name="TextAppearance.Buds.Caption" parent="TextAppearance.Material3.BodySmall">
        <item name="android:textColor">@color/text_secondary</item>
    </style>

</resources>
